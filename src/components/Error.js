import React from 'react';

const Error = () => {
    return (
    <div className="alert alert-danger text-center" role="alert">
        <p>Postal Code does not exist</p>
    </div>
    );
};

export default Error;